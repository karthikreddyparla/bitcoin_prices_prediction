# bitcoin_prices_prediction
Predicted the future of bitcoin prices by applying cross validation and grid search to derive the most efficient model. (ridge, lasso, KNN, linear ,polynomial regression and SVM)
